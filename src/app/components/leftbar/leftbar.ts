import {Component, Input} from '@angular/core';

@Component({
    selector: 'leftbar',
    template: require('./leftbar.html'),
    styles: [require('./leftbar.scss')]
})
export default class Leftbar {
    
}
